# Lean / Lake build artifacts
kanforge/lean-project/.lake/

# Extraction staging (regenerable via tools/extract_modules.py)
kanforge/_extracted/

# Smoke test run artifacts
runs/
kanforge/runs/
kanforge/bench/ablation/

# Node
node_modules/

# Secrets: never commit env files (LLM keys, provider config)
.env
.env.*
!.env.example

# Editor / OS
.vscode/
.DS_Store
Thumbs.db

# HCT lineage material (historical naming intuition only; see kanforge/core for the contracts)
docs/patterns_from_hct.md
src/HCT_primer.txt
